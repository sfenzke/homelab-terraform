apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: longhorn-dahboard
  namespace: longhorn-system
  labels:
    name: longhorn-dashboard
spec:
  rules:
  - host: longhorn.sfenzke.de
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: longhorn-frontend
            port:
              number: 80
